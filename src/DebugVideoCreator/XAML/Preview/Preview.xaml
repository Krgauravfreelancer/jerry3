<UserControl x:Class="VideoCreator.XAML.Preview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideoCreator.XAML"
             xmlns:loader="clr-namespace:VideoCreator.Loader"
             mc:Ignorable="d" >
    <Grid Background="Transparent">
        <Label Content="Preview" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Image Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" x:Name="previewImage" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5"/>
        <loader:LoadingAnimation x:Name="loader" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Hidden"/>
        <ComboBox Grid.Row="1" Grid.Column="0" x:Name="cmbResolution" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,5,5,0"/>
    </Grid>
</UserControl>
